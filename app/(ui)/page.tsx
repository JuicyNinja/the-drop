/**
 * Placeholder. The board (WP-11) and the shipped surfaces (WP-13) replace
 * this. Nothing under app/(ui) may import a Supabase client; UI reaches the
 * product through /v1 like every other client.
 */
export default function Home() {
  return (
    <main>
      <h1>The Drop</h1>
    </main>
  );
}
