/**
 * The Gone stamp (DESIGN-SYSTEM §4.9). Bricolage 800, --oxide, rotated -4°, 70%
 * opacity, overlaid. Color never carries meaning alone — the stamp is the signal,
 * not the hue.
 */
export function GoneStamp() {
  return (
    <span className="gone-stamp coupon" aria-label="Gone">
      Gone
    </span>
  );
}
